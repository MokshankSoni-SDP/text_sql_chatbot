# PostgreSQL Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=text_to_sql_chatbot
DB_USER=postgres
DB_PASSWORD=root
DB_SCHEMA=public

# Groq API Configuration
GROQ_API_KEY=your_groq_api_key_here
GROQ_MODEL=llama-3.3-70b-versatile

# Application Settings
CHAT_HISTORY_LIMIT=5
SCHEMA_MAX_UNIQUE_VALUES=20  # Max unique values to show for text columns
# SCHEMA_EXCLUDED_TABLES=chat_history,audit_log  # Optional: comma-separated list of tables to exclude from enrichment
